FROM python:3.10-slim

WORKDIR /app

# install your Python deps from the local requirements.txt
COPY requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

COPY . /app

CMD ["python3", "agent.py"]